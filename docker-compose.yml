services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: sports_manager
      POSTGRES_USER: sports_manager
      POSTGRES_PASSWORD: sports_manager
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
